# MAP-AREA-CALCULATION-FROM-SATELLITE-PHOTO
This project implements a semantic segmentation pipeline using a U-Net architecture with a ResNet34 backbone. The system processes Sentinel-2 satellite imagery, performs necessary color space transformations, generates a binary mask distinguishing water from land, and algorithmically calculates the surface area.
